{
  "description": "THIS IS ONLY WHERE BECAUSE OF HEROKU PHP DETECTION",
  "require": {
    "php": "^7.4"
  },
  "scripts": {
    "compile": [
      "cd api ; composer install --prefer-dist --no-scripts --no-progress --no-interaction --no-dev",
      "rm -rf vendor"
    ]
  }
}
