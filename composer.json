{
  "require": {
    "php": "^7.4|^8.0"
  },
  "scripts": {
    "post-autoload-dump": [
      "cd api ; composer install --prefer-dist --no-scripts --no-progress --no-interaction --no-dev",
      "rm -rf vendor"
    ]
  }
}
