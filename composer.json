{
  "description": "THIS IS ONLY WHERE BECAUSE OF HEROKU PHP DETECTION",
  "require": {
    "php": "^7.4"
  },
  "scripts": {
    "post-install-cmd": [
      "cd api ; composer install --prefer-dist --no-scripts --no-progress --no-interaction",
      "rm -rf vendor"
    ]
  }
}
